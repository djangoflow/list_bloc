import 'package:flutter/material.dart';

class EmptyTestWidget extends StatelessWidget {
  const EmptyTestWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('EMPTY');
  }
}
